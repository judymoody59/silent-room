<!--
이 파일은 프로젝트가 소유한다. 하네스 갱신이 덮지 않는다.
`.ai/templates/code-reviewer.md` 의 마지막 장이 이 파일을 읽는다.

**이 리포에서만 성립하는 점검을 심각도와 함께 적는다.** 일반론은 적지 않는다 —
계약의 앞 장들이 이미 덮는다. 비워 두면 그 장을 건너뛴다.
-->

| 점검 | 심각도 |
|---|---|
| 자유 텍스트를 받는 필드·파라미터·엔드포인트가 생겼는가 | blocker |
| `Signal` 이 열거형이 아니라 문자열로 열려 있는가 | blocker |
| `domain` 이 `java.*` 외의 것을 import 하는가 | blocker |
| 방 상태를 인스턴스 로컬 필드에 두면서 다중 인스턴스를 전제한 코드가 있는가 | major |
| WebSocket 세션을 닫을 때 재실 정리가 빠졌는가 | major |
| `Message` 라는 이름을 식별자에 썼는가 | minor |

`domain` import 점검은 `script/verify-project.sh` 가 기계로도 막는다. 리뷰에서 겹쳐 보는
이유는 **검사를 우회하는 형태**(리플렉션, 문자열 클래스명, `adapter` 경유 주입)를
기계가 잡지 못하기 때문이다.
